Read `AGENTS.md` and `VISION.md` before edits. Preserve the session-scoped lifecycle boundary.

When agent-facing behavior changes, update every contract surface listed in `AGENTS.md`. Do not call the work complete while README text, tool guidance, packets, tests, and the durable Brain note disagree.

Run `npm run check`, `npm run pack:dry`, and `pi-notes brain check` when a Brain page changes.
